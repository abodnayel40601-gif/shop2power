{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile document containing authentication and profile details.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's registered email address."
        },
        "displayName": {
          "type": "string",
          "description": "The custom username selected by the user."
        },
        "provider": {
          "type": "string",
          "description": "The authentication provider used (e.g., 'password', 'google')."
        }
      },
      "required": ["email", "displayName", "provider"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "User",
      "description": "A collection containing user profile documents keyed by Firebase Auth UID."
    }
  }
}
